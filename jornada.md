# Jornadas do usuário — SmartZap

Este é o catálogo canônico e vivo das jornadas do produto. Ele descreve o que precisa existir e ser testado. Resultados detalhados de cada rodada pertencem a `Auditoria.md`.

Última consolidação: 12/08/2026
Histórico importado de: `docs/auditoria-jornadas.md`

## Regras de manutenção

- Toda nova função, rota, integração, estado ou variação deve entrar aqui na mesma alteração de código.
- Toda lacuna descoberta deve ser adicionada imediatamente como `não testada`.
- Uma jornada só pode ficar `aprovada` quando houver evidência compatível registrada em `Auditoria.md` ou no histórico legado.
- Alterações funcionais reabrem as jornadas afetadas como `corrigida — reteste pendente` até novo teste.
- Os IDs são permanentes. Jornadas removidas ficam `descontinuada`; não reutilizar o ID.

## Catálogo

| ID | Área | Jornada e variações mínimas | Entrada principal | Estado atual |
| --- | --- | --- | --- | --- |
| AUTH-01 | Autenticação | Login correto/incorreto, persistência, expiração e logout | `/login` | aprovada |
| DASH-01 | Dashboard | Carregamento, períodos, atalhos, vazio e erro; respostas interrompidas, truncadas ou estruturalmente inválidas da API devem produzir erro recuperável, nunca dados parciais nem queda do componente. O Dashboard deve oferecer acesso direto ao acompanhamento de conversões quando a integração CAPI estiver disponível, sem apresentar eventos apenas registrados internamente como atribuídos pela Meta. | `/` | aprovada |
| DASH-02 | Dashboard | Resumo operacional simples e responsivo, sem a composição premium/editorial: prioriza o diagnóstico do momento (atividade, estabilidade ou falhas a revisar), identifica campanha e motivo da falha recente com acesso direto ao detalhe, seguido por métricas distintas de envio, entrega, leitura, campanhas e falhas; gráfico de volume e campanhas recentes se reorganizam por breakpoint sem overflow. Preserva estados sem dados, erro, recuperação, período e ação de campanha rápida. | `/` | aprovada |
| DES-01 | Direção visual | Mockup navegável Apple-inspired usado apenas para decisão de produto. A direção foi rejeitada pelo operador e substituída pela restauração visual clássica; a rota pública `/design-preview` foi removida e agora responde pela superfície 404 autenticada, sem integrar o caminho de liberação. | `/design-preview` | descontinuada |
| DES-02 | Direção visual | Aplicar a linguagem visual clássica do SmartZap nas rotas autenticadas: superfícies escuras diretas, navegação lateral utilitária, ações verdes retangulares, cartões sem efeitos editoriais e estados responsivos; preservar as jornadas operacionais e os estados responsivos. | aplicação autenticada | aprovada |
| DES-03 | Direção visual | Manter os CTAs primários da linguagem clássica compactos, alinhados ao grid e com contraste legível entre fundo verde, texto e ícone; preservar hierarquia tipográfica, foco visível, estados desabilitados e responsividade, sem reintroduzir vidro, brilhos ou sombras editoriais do redesign revertido. | aplicação autenticada | aprovada |
| NAV-01 | Navegação global | Menu e breadcrumb; funções contextuais de público não ocupam item próprio no menu global; Tutoriais abre ajuda inline; tema persiste; Alertas refletem a saúde real, oferecem rotas de correção quando há pendência e confirmam explicitamente quando a operação está pronta; o antigo botão de Modo Desenvolvedor foi removido porque não acionava ferramenta alguma; rota inexistente ou legado removido apresenta destino explícito, nunca uma casca vazia. O acesso a Conversões fica disponível como superfície analítica própria e preserva o comportamento mobile do menu. | aplicação | aprovada |
| ONB-01 | Onboarding | Primeira instalação exibe checklist idempotente derivado da saúde real de credenciais, Meta, webhook e templates no Dashboard e em Configurações; não obstrui Inbox, campanhas ou demais superfícies operacionais; permite retomar, dispensar e navegar para a correção | entrada autenticada, `/` e `/settings` | aprovada |
| INST-01 | Entrada pública do instalador | Antes de existir qualquer Worker de destino, o usuário abre uma página pública e a rota `/install` sem formulário, secret, analytics ou armazenamento local; ambas encaminham ao seletor `https://instalar.escoladeautomacao.com/smartzap/`, que mantém duas modalidades claramente separadas: fork próprio recomendado para produção e instalação rápida OAuth de versão fixa para avaliação. O domínio é um portal genérico de instaladores da Escola de Automação e cada sistema ocupa uma rota própria; o SmartZap mantém fork, OAuth, callback, APIs, cookie, logo e release sob `/smartzap`, sem compartilhar autorização ou sessão com produtos futuros. A rota pública `/guia.md` entrega em Markdown puro o guia canônico e autônomo para equipes sem contexto prévio, com MIME type explícito, leitura cross-origin e sem iniciar OAuth, criar sessão ou expor segredos. Nenhuma superfície pode chamar Deploy Button de fork, prometer atualização automática, SLA ou manutenção gratuita, nem gerar credenciais fora da modalidade efetivamente escolhida. | página pública, `/guia.md`, `/install` e `instalar.escoladeautomacao.com/smartzap/` | corrigida — reteste pendente |
| INST-02 | Provisionamento Cloudflare | O provisionador próprio deve usar OAuth com seleção explícita da conta e criar Worker/assets, D1, R2, seis Queues/DLQs, três consumidores, dois Durable Objects, Workflow de campanhas, Workflow isolado de diagnóstico, Cron, rate limit e Workers AI. Antes de alterar a conta, `/api/plan` deriva todos os nomes do prefixo aleatório, consulta os recursos e classifica cada um como `create`, `reuse` pelo mesmo ledger ou `blocked`; qualquer recurso arbitrário ou sem ledger compatível interrompe a instalação. A release imutável valida manifesto, assets, módulos e baseline D1 por SHA-256, aplica a baseline final em uma transação, configura bindings e somente então libera tráfego. O resolvedor do bucket de releases deve extrair a chave R2 depois do segmento `/release/` mesmo quando o portal está publicado em subrota como `/smartzap`, sem duplicar o prefixo do produto; artefato ausente ou checksum divergente deve interromper e acionar rollback integral. Falha antes da liberação remove somente os recursos criados pela rodada; uma nova sessão autorizada da mesma conta pode retomar sem duplicar versão ou consumidor. Quando o R2 ainda não estiver ativado, o plano deve parar antes de criar qualquer recurso, explicar em português que a Cloudflare exige ativação e meio de pagamento mesmo dentro da franquia gratuita e abrir a conta correta para ativação; depois disso, o usuário retorna e confere novamente o mesmo plano. O `/setup` consulta cada Queue/DLQ, Workflow e Cron e explica em linguagem comum qualquer ausência; recurso opcional não bloqueia o núcleo. | provisionador OAuth, `/api/plan`, `/api/install`, APIs Cloudflare e `/setup` | corrigida — reteste pendente |
| INST-03 | Cofre de credenciais | Guardar credenciais externas somente cifradas em D1 com AES-256-GCM, IV aleatório e AAD versionado; manter as chaves exclusivamente como secrets do Worker; nunca devolver plaintext pelas APIs ou logs. Rotação usa chave temporária e token exclusivo, bloqueia gravações concorrentes, recifra todos os registros em uma transação e só reabre após promoção/finalização. Uma execução órfã pode ser recuperada depois da janela de segurança somente se todos os registros ainda abrirem com a chave ativa; perda da chave exige recadastramento explícito. | D1, secrets do Worker, `/api/setup/vault/rotate`, `/api/setup/vault/recover` e `/api/setup/vault/finalize` | aprovada |
| INST-04 | Assistente de configuração | Verificar recursos, cadastrar e validar Meta/WhatsApp, configurar automaticamente no aplicativo, WABA e número o webhook desta instalação usando o Verify Token cifrado no cofre, sincronizar templates, enviar uma mensagem real somente a contato autorizado e confirmar automaticamente os três estados `sent`, `delivered` e `read` pela entrada oficial e Queue, tolerando callbacks fora de ordem sem exigir consulta manual e sem liberar quando qualquer estado estiver ausente. Os status da mensagem especial do setup devem ser preservados como evidência terminal, sem entrar no backlog de reconciliação de campanhas; órfãos de campanhas reais continuam pendentes para retry. A configuração automática deve impedir que um override antigo desvie callbacks, nunca devolver plaintext e oferecer verificação manual somente como diagnóstico. Secret ausente, senha incorreta, recurso não provisionado, Meta recusada, webhook inválido, ausência de template, mensagem ainda não lida e instalação interrompida precisam informar em linguagem comum o que aconteceu, por que bloqueia e como corrigir. A tela deve preservar o progresso, levar o usuário à etapa correta, verificar envio/entrega/leitura automaticamente e nunca expor uma ação técnica `delivered/read`. O núcleo deve redirecionar para `/setup` até todos os gates obrigatórios permanecerem verdes. | `/setup`, Meta Cloud API, webhook e Queues | aprovada |
| INST-05 | Módulos opcionais | Instalar IA, CAPI, calendário e recursos avançados desligados por padrão; ausência de credenciais opcionais não bloqueia WhatsApp, Inbox, campanhas, contatos e templates, e a interface não apresenta o módulo como pronto antes da ativação própria. | `/setup`, Configurações e módulos opcionais | corrigida — reteste pendente |
| INST-06 | Homologação física limpa | Repetir a instalação completa sem CLI e sem GitHub Actions pelo menos três vezes em contas Cloudflare limpas, incluindo duas contas gratuitas e uma paga; cobrir a ativação explícita do R2 em conta gratuita sem pagamento herdado, colisões, interrupção, retomada, falhas de provisionamento, Meta real, cleanup e ausência de recursos residuais. O certificador deve exigir fingerprints de três contas distintas, a mesma release imutável, relatórios aprovados de baseline, provisionamento e setup completo para cada instalação e provas físicas separadas de colisão, retomada e cleanup; não aceita relatório sintético nem reutilização da mesma conta. | contas Cloudflare limpas, matriz `qa:install:canary matrix` e Meta real | em teste |
| INST-07 | Gate de distribuição pública | Auditar árvore e todo o histórico Git sem exibir segredos, remover dados/configuração da conta de origem e rotacionar qualquer credencial comprometida antes de tornar o repositório público. O selo de instalação simples e o botão público permanecem bloqueados enquanto `INST-01` a `INST-06` não estiverem aprovadas. | GitHub público, `/install` e README | bloqueada |
| INST-08 | Autorização Cloudflare | Autorizar o provisionador SmartZap por OAuth Authorization Code com PKCE, `state` de uso único, cookie `HttpOnly` restrito a `/smartzap`, seleção explícita da conta e escopos mínimos. O cliente OAuth deve possuir nome, logo público próprio em `/smartzap/logo.svg`, domínio de publicação `instalar.escoladeautomacao.com` verificado e visibilidade `public`, pois clientes `private` só podem ser autorizados por membros da conta Cloudflare que os criou; a homologação precisa incluir uma conta externa que não seja membro da conta desenvolvedora. Tokens de acesso e renovação permanecem cifrados no D1 do provisionador; não podem aparecer em URL, frontend ou logs. Quando a autorização não permitir listar contas, aceitar Account ID informado pelo usuário somente após uma chamada de validação não destrutiva. Desconectar deve revogar o token sem apagar recursos instalados. Sessões abandonadas devem expirar em até 30 minutos, tentar revogação remota e apagar obrigatoriamente o token cifrado local por limpeza periódica, mesmo se o provedor já estiver indisponível. | `/smartzap/oauth/start`, `/smartzap/oauth/callback`, `/smartzap/api/account`, `/smartzap/logo.svg` e Cron de limpeza | corrigida — reteste pendente |
| INST-09 | Plano e execução idempotente | Antes do deploy, produzir um plano somente leitura que classifique cada recurso como criar, reutilizar com marcador compatível ou bloquear por colisão. A execução nova deve criar D1, R2 e seis Queues/DLQs, aplicar em uma única transação um único baseline final versionado e verificado contra o schema de desenvolvimento — sem transportar nem reproduzir as 51 migrações históricas —, registrar uma única versão inicial e recusar banco parcial ou checksum divergente. Deve carregar assets e uma versão imutável do Worker, declarar Durable Objects, criar os dois Workflows, reconciliar consumidores sem duplicá-los, configurar Cron e subdomínio e somente então liberar tráfego. Somente alterações posteriores ao baseline podem entrar como upgrades imutáveis separados. Interrupção deve permitir retomada por uma nova sessão autorizada da mesma conta e identificador: a interface precisa aceitar o identificador ou arquivo de recuperação salvo, invalidar qualquer plano calculado antes de uma alteração e o backend só pode reutilizar recursos que constem no ledger daquela mesma conta e instalação, mantendo lease contra concorrência. Falha antes da liberação executa rollback apenas dos recursos criados pela rodada e nunca remove recurso preexistente. | `/api/plan`, `/api/install`, Account API e release imutável | aprovada |
| INST-10 | Segredos efêmeros do primeiro deploy | A senha administrativa e a `SMARTZAP_VAULT_KEY` são criadas no navegador, entram uma única vez no corpo HTTPS da execução e são gravadas diretamente como bindings secretos da versão de destino. O provisionador não persiste esses valores no próprio D1, não os inclui em estado, plano, erro ou log e exige nova digitação para retomar depois de uma interrupção que ocorra antes do upload da versão. | navegador, `/api/install` e Worker Version API | aprovada |
| INST-FORK-01 | Fork próprio | Na modalidade recomendada, o usuário inicia por um fork verdadeiro do GitHub, confirma que o repositório pertence à própria conta e o importa no Workers Builds. Antes de liberar a etapa Cloudflare, `/smartzap/fork/` consulta a API pública do GitHub e exige `fork=true`, parent exato `thaleslaray/smartzap-cloudflare`, visibilidade pública e branch padrão `main`. A verificação executável `npm run fork:verify -- --owner=SEU_OWNER --prepare --protect-main` repete essas provas, cria somente `upstream-sync` a partir do SHA de `main` e protege `main` contra force-push, exclusão ou merge sem PR e check `validar`. O fork usa `sync/vX.Y.Z` para propostas oficiais e `customer/*` somente para customizações reais; um push autorizado em `main` publica somente na Cloudflare do proprietário. Deploy Button, cópia independente ou repositório apenas renomeado não satisfaz esta jornada. | `/smartzap/fork/`, GitHub, `fork:verify` e Workers Builds | em teste |
| INST-FORK-02 | Bootstrap do fork | O primeiro build de um fork deve derivar nomes isolados de `SMARTZAP_INSTALL_ID`, criar ou retomar com segurança Worker/assets, D1, R2, seis Queues/DLQs, consumidores, dois Durable Objects, Workers AI, Cron, rate limit e dois Workflows, aplicar somente a baseline final e cadastrar `MASTER_PASSWORD` e `SMARTZAP_VAULT_KEY` como secrets. Colisão arbitrária, D1 parcial, recurso pertencente a outro Worker ou secret inválido interrompe sem sobrescrever; repetir com o mesmo identificador é idempotente. Produção e staging usam projetos, tokens e recursos físicos diferentes. O comando de branch usado pelo Workers Builds falha fechado sem `WORKERS_CI_BRANCH`; `main` só publica no Worker-base conectado, `staging/*` só publica no Worker `-staging` conectado e qualquer combinação cruzada ou outra branch valida sem chamar Wrangler. A retomada remove temporariamente apenas consumidores das três Queues que já pertençam ao Worker exato, recusa consumidor estrangeiro/HTTP, restaura os consumidores próprios se o deploy falhar e exige uma única identidade JSON de deploy, mesmo quando o Wrangler mistura diagnóstico no arquivo estruturado. | Workers Builds, `npm run fork:deploy`, `npm run fork:branch` e `npm run fork:preview` manual | aprovada |
| INST-FORK-03 | Identidade e setup do fork | Depois do deploy, `/setup` deve mostrar versão, commit, schema e canal instalados, verificar todos os recursos obrigatórios, configurar Meta/WhatsApp e só liberar o núcleo após template e mensagem real `sent → delivered → read`. A identidade exibida precisa corresponder aos metadados persistidos no D1 e aos bindings imutáveis da release, sem revelar secrets. | `/setup`, D1 e Worker vars | aprovada |
| INST-QUICK-01 | Instalação rápida fixa | A rota `/smartzap/quick/` preserva o provisionador OAuth atual e informa, antes da autorização, a versão exata instalada, ausência de Git e atualizações automáticas, responsabilidade do proprietário, retenção/expiração OAuth e recomendação do fork. OAuth, plano, instalação, retomada e revogação continuam isolados sob a rota rápida; a criação do fork não usa nem altera essa sessão. A release rápida deve transportar versão, commit, schema e canal no manifesto validado, gravar a mesma identidade no D1 e declará-la como bindings imutáveis do Worker; depois da instalação, `/setup` precisa exibir exatamente esses quatro valores, sem aceitar `não identificado` como sucesso. | `/smartzap/quick/`, OAuth Cloudflare, provisionador, D1 e `/setup` | em teste |
| UPD-01 | Proposta de atualização | O workflow opcional pode receber uma tag SemVer exata ou, na execução diária, consultar a release `stable` oficial mais recente; ausência de release estável encerra sem alterar o fork. A tag baixada é verificada contra a âncora `release/allowed_signers` já aprovada no fork — nunca contra uma chave fornecida pela própria atualização. Para forks com histórico anterior à baseline pública, a proposta usa `bridge/vX.Y.Z`: a árvore precisa ser idêntica à tag assinada e o histórico precisa incluir o `main` do fork. A proposta executa metadata, typecheck, testes e build e abre PR contra `main` materializando changelog, migrations, incompatibilidades e rollback. Ela não resolve conflitos, não migra D1, não faz merge e não publica produção. Dependabot também apenas propõe PRs para npm e GitHub Actions, sem merge ou deploy automático. No Workers Builds, branches de proposta, `customer/*` e branches desconhecidas encerram após validação sem chamar Wrangler; apenas `main` publica produção e `staging/*` pode criar staging físico isolado. | `.github/workflows/upstream-sync.yml`, `.github/dependabot.yml`, `npm run fork:branch` e GitHub PR | corrigida — reteste pendente |
| UPD-02 | Homologação de atualização | Atualizações patch devem ser comprovadas em fork sem customização, com customização sem conflito e com conflito intencional. O ensaio isolado `npm run qa:fork:updates` precisa provar que customizações não conflitantes são preservadas e que conflitos interrompem o fluxo sem resolução automática, restaurando o fork anterior após cancelamento. O proprietário ainda revisa o PR, captura bookmark D1, testa a tag em staging físico, resolve conflitos manualmente e só então aprova o merge para produção. A prova física deve registrar PR cruzado do upstream, merge no branch de staging, versão Cloudflare exata, health de staging e produção, rollback para a versão imediatamente anterior e restauração da candidata em menos de dez minutos. | Git temporário isolado, fork GitHub, staging e Workers Builds | aprovada |
| MIG-01 | Migrações de forks | Cada migration pós-baseline é append-only e imutável e declara origem/destino, compatibilidade, indisponibilidade, destrutividade, prechecks, postchecks e recuperação em `release/migrations.json`. A instalação registra versão/commit/schema/checksum, captura bookmark antes de escrita, rejeita arquivo ausente ou checksum divergente e comprova sucesso, falha controlada e retomada sem schema parcial. Migrações internas de compatibilidade só entram no ledger quando alteram fisicamente o schema; um cron executado sobre a baseline final não pode fabricar uma migration aplicada. Se um rollback para runtime histórico recriar o marcador sintético interno `0035`, a próxima migration pública remove somente esse registro, exige bookmark e reprova o postcheck enquanto o ledger público não contiver exatamente a cadeia declarada. | `fork-migrations/`, `release/migrations.json`, D1 e staging | aprovada |
| ROLLBACK-01 | Rollback autogerenciado | O proprietário consegue voltar o Worker à versão anterior e, quando houve mudança de schema, restaurar o D1 pelo bookmark/Time Travel em menos de dez minutos. A restauração é exata e pode recuperar inclusive um marcador sintético antigo; nesse caso, o tráfego continua fechado até a release atual ser reaplicada e passar nos postchecks. O runbook distingue R2, Queues e Durable Objects, que não voltam com a versão do Worker, exige postchecks de `/setup`, health, filas e webhooks e comprova que dados/resíduos não foram silenciosamente perdidos. | Versions & Deployments, D1 Time Travel e runbook | aprovada |
| SEC-RELEASE-01 | Cadeia de releases | A distribuição pública e cada release imutável devem conter licença, contratos, tag SemVer assinada por chave oficial permitida, commit, canal, changelog, pacote derivado da tag exata, manifesto anexado com inventário e árvore Git, `SHA256SUMS` com assinatura destacada, migrations, requisitos, breaking changes e recuperação; dependências críticas e actions ficam pinadas. Scanner da árvore e do histórico não pode encontrar segredo, dado pessoal ou ID da Escola de Automação; o deploy também recusa artefato incompleto ou qualquer `.dev.vars` gerado dentro de `dist`. O verificador deve falhar diante de pacote, manifesto, checksum ou assinatura adulterados. Secrets existem somente na Cloudflare, OAuth rápido é revogado após provisionar e vulnerabilidades seguem advisory privado. | repositório público, GitHub Release, manifesto anexado, `SHA256SUMS`, assinatura e scanner | aprovada |
| LEG-01 | Privacidade pública | Exibir política pública, legível, responsiva e sem autenticação com controlador, categorias de dados, finalidades, fornecedores, conservação, segurança, direitos e minimização específica da CAPI for Business Messaging. Na distribuição autogerenciada, o controlador é o proprietário de cada instalação; o texto não pode atribuir ao upstream acesso, operação ou controle automático sobre dados de terceiros. | `/privacy` | aprovada |
| LEG-02 | Exclusão de dados | Exibir instruções públicas, responsivas e sem autenticação para solicitar exclusão, verificar titularidade, acompanhar o pedido e distinguir dados do SmartZap daqueles controlados pela Meta/WhatsApp. Na distribuição autogerenciada, o canal indicado deve pertencer ao proprietário da instalação, sem direcionar titulares de terceiros ao mantenedor do upstream. | `/data-deletion` | aprovada |
| PWA-01 | Aplicativo instalável | Manifesto, instalação, atualização e service worker; banner aparece somente quando o navegador oferece instalação ou há atualização. O service worker controla somente navegações do shell e deixa assets versionados no caminho de rede/cache HTTP do navegador, evitando interceptar chunks em Firefox. Durante a propagação de um deploy publicado, incompatibilidade entre o HTML anterior e chunks da versão nova dispara uma única recuperação automática com cache bust, preservando rota e parâmetros; em desenvolvimento a recuperação automática fica desativada para não mascarar falhas de módulo nem interromper navegações. O gate remoto exige três provas consecutivas de health, autenticação e coerência entre shell e assets antes de avançar. | aplicação | aprovada |
| CNT-01 | Contatos | Lista, busca, ordenação, filtros expansíveis, paginação, perfil e estado vazio após exclusão, com paridade funcional e visual ao original. A confirmação de exclusão individual mantém ícone e rótulo da ação destrutiva centralizados como um único grupo em desktop e mobile. A barra de filtros distribui botão, status e tag em uma grade fluida na largura de 360 px e preserva as larguras compactas em telas maiores, sem overflow. Loading, vazio, erro e retentativa têm E2E nos três motores; comparação visual usa fixture equivalente e sete viewports. Ações de modal inexistentes no legado mobile são cobertas funcionalmente no migrado e registradas como limitação da referência. | `/contacts` | aprovada |
| CNT-02 | Contatos | Criar manual não infere opt-in: sem declaração explícita, o contato nasce como `unknown` e permanece inelegível para disparos até consentimento válido. Criar, editar, nome, telefone, e-mail, tags, campos personalizados, opt-out e supressão; modais e estados com paridade ao original. | `/contacts` | aprovada |
| CNT-03 | Contatos | Importar CSV, validar, tratar duplicatas/falhas e exportar; sem declaração explícita, a importação mantém contatos como `unknown`, sem elegibilidade para disparos. Modais e feedbacks têm paridade ao original. | `/contacts` | aprovada |
| CNT-04 | Contatos | Operações em massa e consistência do perfil. A ficha avançada de memória/histórico foi descontinuada para preservar o fluxo do SmartZap original; clicar no contato abre a edição compacta. | `/contacts` (modais) | aprovada |
| SEG-01 | Públicos salvos | Na etapa Público da campanha, combinar tags, DDI e DDD, dar nome e salvar o público para reutilização; ao salvar, o público reaplica exatamente seus critérios. A confirmação HTTP 201 atualiza imediatamente a lista local, sem o item recém-criado desaparecer quando uma consulta iniciada antes do salvamento terminar depois. Um público selecionado pode ser excluído inline, com confirmação explícita no mesmo bloco. A rota legada `/segments` não é item de navegação global. | `/campaigns/new` | aprovada |
| SEG-02 | Segmentação geográfica | Selecionar e buscar todos os países/territórios do catálogo de DDI, as 27 UFs e os 67 DDDs brasileiros; aplicar alcance (OR) e precisão (AND), preservar a regra especial do DDD 61 compartilhado por DF/GO e informar que países que compartilham DDI não são distinguíveis apenas pelo prefixo. A seleção de UF/DDD usa uma grade regular de três colunas, com 27 UFs em nove linhas completas de chips idênticos; fica desabilitada até Brasil ser selecionado. | `/campaigns/new` | aprovada |
| TMP-01 | Templates | Listar, buscar, filtrar, status e prévia; sincronizar manualmente e automaticamente ao salvar Phone ID/WABA válidos. Os filtros de categoria e status quebram linha responsivamente, sem barras de rolagem internas expostas ou categorias cortadas. Falha da sincronização automática preserva a configuração e a cópia local, informa o operador e permite nova tentativa manual. Templates avançados ou de Autenticação recebidos da Meta permanecem visíveis e íntegros, mas não são anunciados como editáveis ou enviáveis pelo fluxo simples. | `/templates` e `/settings` | aprovada |
| TMP-02 | Templates | Criar rascunhos de Marketing e Utilidade com navegação no rodapé, prévia viva sem ação duplicada, inserção de variável na posição do cursor, idiomas, corpo estático/variável, rodapé e botões de resposta rápida, URL estática/dinâmica e telefone. Autenticação não aparece como categoria criável enquanto o contrato especializado de OTP estiver fora do escopo. No celular, usar a ação flutuante de prévia e manter os três passos legíveis dentro do viewport. Antes de avançar ou publicar, aplicar as regras oficiais da Meta: corpo com até 1.024 caracteres, exemplo para cada variável, parâmetros posicionais iniciando em `{{1}}` e sem lacunas, proibição de variável no início ou fim, limites e combinações dos botões suportados. | `/templates/drafts/new` | aprovada |
| TMP-03 | Templates | Editar Marketing e Utilidade sem apagar componentes ou botões avançados não representados pela interface simplificada; validar, publicar na Meta, acompanhar status e excluir. Rascunhos de Autenticação ou com contrato não representado ficam protegidos contra edição/publicação genérica. | `/templates/drafts/:id` | aprovada |
| TMP-05 | Templates | Paridade completa de criação visual do editor rico original — formato de parâmetros, localização, carrossel e botões COPY_CODE/OTP/FLOW — foi retirada do escopo desta versão. O editor simplificado não anuncia esses controles; recursos avançados recebidos da Meta continuam protegidos pela jornada TMP-06. | `/templates/drafts/new` e `/templates/drafts/:id` | descontinuada |
| TMP-06 | Templates | Ao editar texto em um rascunho avançado de Marketing ou Utilidade, preservar sem alteração cabeçalho de localização, carrossel, exemplos e botões COPY_CODE, OTP e FLOW que o editor simplificado não representa visualmente. Templates de Autenticação sincronizados ficam somente para leitura até existir editor OTP próprio. | `/templates/drafts/:id` | aprovada |
| TMP-07 | Templates | Processar pela entrada HTTP assinada os eventos oficiais de ciclo de vida do template: status e motivo/recomendação, qualidade, componentes e categoria programada/efetivada; manter idempotência e ordem temporal, persistir o estado `PENDING` imediatamente após a submissão, sincronizar componentes pela Graph API, bloquear imediatamente o envio quando o estado não permite uso, reabilitar somente após estado oficial elegível e expor diagnóstico acionável na interface. A prova real usa somente fixture AUTOQA em staging e troca temporariamente o callback global do app por escopo operacional fechado, com restauração convergente para produção; estados que degradariam deliberadamente a qualidade da WABA são comprovados por contrato oficial assinado, sem provocar abuso no provedor. | `/webhook`, Queue, `/templates`, campanhas e Inbox | aprovada |
| TMP-04 | Templates | Clonar templates de Marketing e Utilidade, resolver nomes duplicados e preservar componentes/idioma; recusar clonagem genérica de Autenticação com orientação explícita | `/templates` | aprovada |
| PRJ-01 | Projetos | Criar projeto pela interface, configurar campos, revisar, gerar ativos e persistir o lote de templates. Cobrir Marketing e Utilidade, idiomas suportados, quantidades e limites, falhas do gerador, preservação entre etapas, seleção parcial e validação oficial dos componentes antes de persistir. As rotas canônica e legada precisam apresentar o mesmo estado. | `/templates/projects/new` e `/templates/new` | aprovada |
| PRJ-02 | Projetos | Listar, buscar, reabrir, renomear, adicionar, editar, filtrar, selecionar, persistir, retomar e excluir projeto pela interface real, cobrindo vazio, loading, erro, retentativa, confirmação destrutiva, responsividade, acessibilidade e consistência após recarregar. As rotas canônica e legada precisam apresentar o mesmo estado. | `/templates/projects`, `/templates/projects/:id` e `/templates/:id` | aprovada |
| PRJ-03 | Projetos/Meta | Publicar itens elegíveis de um projeto na Meta com validação equivalente ao editor simples, exemplos de todas as variáveis, idempotência contra clique/retry concorrente, tratamento de resposta parcial e ausência de ID remoto; sincronizar aprovação, análise e rejeição sem permitir edição ou exclusão local insegura de ativo remoto. A prova real usa canários AUTOQA permitidos, destinatários autorizados e limpeza comprovada. | `/templates/projects/:id`, API de projetos e Graph API | aprovada |
| PRJ-04 | Projetos/Resiliência | Preservar integridade sob lote máximo, projetos e itens numerosos, salvamentos simultâneos, duplo envio, timeout e recuperação do provedor; manter ordenação e datas coerentes, impedir nomes remotos duplicados e terminar testes temporários sem resíduos locais ou remotos. | API de projetos, D1 e Graph API | aprovada |
| CMP-01 | Campanhas | Lista, busca, filtros, pastas, métricas e estados vazios. Status, pasta e tags usam popovers internos uniformes, sem selects nativos; os menus não devem vazar nem cortar conteúdo na largura reduzida. Uma campanha tecnicamente concluída, mas com falhas, aparece como `Concluída com falhas` em alerta, nunca como sucesso pleno; o filtro `Falhou` inclui tanto o estado técnico `failed` quanto qualquer campanha com falhas registradas. A lista mostra somente Entrega Meta, deixando explícito que é confirmação por webhook; o progresso de envio (aceite/erro do lote) fica no detalhe e Eventos operacionais, evitando coluna vazia/redundante. | `/campaigns` | aprovada |
| CMP-02 | Campanhas | Configuração de templates de Marketing e Utilidade: um campo por `{{n}}`, fontes nome/telefone/e-mail/campo personalizado/fixo, fallback e prévia única; bloquear avanço com variável sem mapeamento. Template de Autenticação ou outro contrato dinâmico não representado deve ser recusado antes da criação da campanha, com orientação explícita. Quando um contato não tiver valor, identificar a variável e a área afetada sem expor chaves internas como `body.3`. | `/campaigns/new` | aprovada |
| CMP-03 | Campanhas | Público por todos, contatos, teste e segmento; opt-out, supressão e duplicidade. A segmentação rápida inclui o catálogo completo de países/territórios por DDI e as 27 UFs/67 DDDs do Brasil, com busca e sem confundir países que compartilham um código internacional. Ao selecionar `Teste`, usa automaticamente o único destinatário configurado, prepara seu opt-in/evidência e renderiza a prévia real sem pedir nova seleção. O contrato de contatos explícitos aceita até três destinatários, alinhado ao limite máximo do canário real; o reteste real comprovou precheck, dispatch e aceite dos três em uma única campanha. | `/campaigns/new` | aprovada |
| CMP-04 | Campanhas | Validação, estimativa, agenda, fuso, rascunho e disparo autorizado; orientar correção conforme o motivo do contato ignorado | `/campaigns/new` | aprovada |
| CMP-05 | Campanhas | Detalhe, lotes, métricas, status, falha, pausa e cancelamento. Uma campanha concluída com falhas deve manter o estado técnico, mas exibir `Concluída com falhas` em alerta e apontar a falha nos logs; dados técnicos de throughput e duração ficam em Eventos operacionais/Performance, não no fluxo principal da campanha | `/campaigns/:id` | aprovada |
| CMP-06 | Campanhas | Pastas, cores, tags, duplicação, relatório CSV e reenvio de ignorados. Ao clonar, preserva template, mapeamento, pasta, tags e definição de público; recalcula a contagem elegível atual sem copiar envios/histórico. Quando não houver tags, o filtro e a ação por campanha oferecem criação direta em Organizar Campanhas na aba Tags. Em cada campanha, mover para pasta usa menu próprio acessível, com “Sem pasta”, indicação da pasta selecionada e sem menu nativo do navegador; a seleção de tag apresenta o novo estado imediatamente, preserva-o durante a atualização e reverte em caso de erro; exclusão individual e em lote usam modal interno com confirmação explícita; uma seleção com campanha em envio ou pausada é bloqueada integralmente. | `/campaigns` e `/campaigns/:id` | aprovada |
| PRC-01 | Preços | Selecionar rate card por país, categoria, moeda e vigência; em Configurações, importar automaticamente as tarifas BRL oficiais da Meta/WhatsApp, validá-las e ativá-las, informando claramente se houve atualização ou se a tabela já estava vigente; upload CSV somente como contingência | API de pricing/campanha e `/settings` | aprovada |
| PRC-02 | Preços | Calcular tiers, estimativa por mensagem e custo do lote | API de pricing/campanha | aprovada |
| PRC-03 | Preços | Converter USD/BRL e usar a última cotação válida como fallback | API de câmbio/campanha | aprovada |
| PRC-04 | Preços | Registrar snapshot estimado e confirmação posterior da Meta; no detalhe, exibir ambos em faixa compacta, sem competir com métricas e logs operacionais. | detalhe da campanha | aprovada |
| PRC-05 | Preços | Exibir indisponibilidade sem inventar taxa ou custo | wizard/detalhe | aprovada |
| INB-01 | Inbox | Lista, busca, filtros, conversa, não lidas e atualização em tempo real. Quando houver origem CTWA, o detalhe mostra a procedência do anúncio e os touchpoints associados sem expor integralmente o `ctwa_clid`; conversa orgânica ou anúncio sem identificador mantém estado explícito e não inventa atribuição. | `/inbox` | aprovada |
| INB-02 | Inbox | Enviar/receber texto, mídia e templates suportados de Marketing ou Utilidade; resolver corpo e URL variáveis, confirmar o envio, materializar status e falhas e respeitar a janela Meta. Autenticação e contratos dinâmicos não representados devem ser recusados antes de chamar o provedor. | `/inbox/:id` | aprovada |
| INB-03 | Inbox | Respostas rápidas, labels, notas, handoff, atendente e histórico. A criação ou remoção de atendente confirmada pela API não pode ser revertida visualmente por uma leitura da lista iniciada antes da mutação | `/inbox/:id` | aprovada |
| ATD-01 | Atendimento | Fila, atribuição, disponibilidade e conversa por link direto; link ausente/inválido e falha de resposta precisam ser anunciados como alerta, enquanto validação em andamento usa status não intrusivo | `/atendimento` | aprovada |
| ATD-02 | Atendimento | Criar, editar, ativar/desativar atendentes e permissões | `/settings/attendants` | aprovada |
| KNO-01 | Conhecimento | Criar, editar, upload, indexar, buscar, reindexar e excluir | `/knowledge` | aprovada |
| AI-01 | IA | Configuração de um provedor, limites, indisponibilidade e erro; o timeout do provider cobre respostas fundamentadas lentas sem deixar chamadas penduradas | `/settings/ai` | aprovada |
| AI-02 | IA | Criar agente, instruções, RAG, ativar/desativar, testar e excluir; o prompt fundamentado impõe opt-in/consentimento para disparos e não permite inferir consentimento por importação; erro transitório ou saída vazia admite uma segunda tentativa finita, sem repetir timeout | `/settings/ai/agents` | aprovada |
| AI-03 | IA | Sugestão na Inbox, contexto, memória, RAG, segurança e handoff; respostas fundamentadas aguardam o limite operacional de 30 segundos, repetem apenas falha transitória/saída vazia uma vez, rejeitam reciclagem semântica da última resposta e falham fechadas depois disso | `/inbox/:id` | aprovada |
| AI-04 | IA | Prompt injection, isolamento entre contatos, fonte ausente, timeout e falha segura do provedor; importação ou ausência de opt-out nunca vira consentimento, disparo em massa exige evidência, pedidos para expor tokens, senhas, chaves, credenciais ou segredos são recusados por política determinística sem refletir valores, e falhas do provider ficam identificadas por código sanitizado no simulador | Inbox, agentes e conhecimento | aprovada |
| AI-05 | IA comercial | Configurar identidade, prompt, regras de qualificação, FAQ operacional, RAG, consentimento, campanha, objeção comercial e handoff por preço/contrato; validar no canal real. O prompt fundamentado exige consentimento explícito e segmentação antes de orientar disparos; preço, prazo, proposta, pedido de humano e estados canônicos de entrega têm uma política determinística compartilhada pelo envio automático e pelo simulador do agente, evitando respostas contraditórias entre os dois canais. Pedidos de disparo para toda uma lista sem evidência de consentimento e promessas contratuais de disponibilidade absoluta também são decisões determinísticas e fail-closed, sem depender da resposta variável do provedor. Perguntas diretas por “status/estados de mensagem, envio ou entrega”, mesmo sem citar previamente um estado, devem devolver os identificadores exatos `sent`, `delivered`, `read` e `failed`. Respostas fundamentadas acima de 700 caracteres falham fechadas e são refeitas, nunca cortadas no meio. Organizar o Inbox, explicar risco de bloqueio e recusar opt-in automático de lista importada usam respostas determinísticas ligadas às capacidades e políticas documentadas, impedindo invenção de filtros/filas, garantia de bloqueio e contato com lista sem consentimento. A correção passou 9/9 casos focais e 84/84 sessões reais no Workers AI do staging, seguida de leitura semântica independente 84/84. O mesmo runtime foi promovido à produção como `928fcd00-3bc3-4494-b4bd-f5cf5095c51f` e o canário real `AUTOQA_AI_V4_PRODUCTION_WAHOOKS_20260809` comprovou no número autorizado `…9966` uma pergunta sobre lista importada sem opt-in, resposta segura exata, um único inbound/outbound, status `read`, recepção no WAHooks e restauração integral do estado anterior da conversa. | `/settings/ai/agents`, `/knowledge` e `/inbox/:id` | aprovada |
| INB-04 | Inbox/IA | Configurar timeout humano, retenção, chave global de Atendimento IA e confirmar que desligar a chave impede a automação autônoma em conversas já habilitadas | `/inbox` e `/settings/ai/agents` | aprovada |
| AI-06 | IA | Desativar um agente e confirmar que conversas já atribuídas deixam de gerar, aprovar e enviar respostas automáticas; reativar e confirmar retomada controlada | `/settings/ai/agents` e `/inbox/:id` | aprovada |
| AI-07 | IA | Paridade entre saúde central, estado do agente, documentos indexados, metadados exibidos e prontidão real do provedor | `/settings/ai`, `/settings/ai/agents` e `/knowledge` | aprovada |
| AI-08 | IA na Inbox | Cada controle do cabeçalho deve ter semântica própria: contexto/memória, modo/agente e menu de ações; a cópia deve distinguir rascunho assistivo de automação autônoma | `/inbox/:id` | aprovada |
| WHK-01 | Teste de IA via WAHooks | Usar uma conexão WAHooks autorizada apenas como número de teste: enviar texto ao número oficial Meta, receber o inbound no webhook Meta, acionar IA/RAG na conversa e devolver a resposta pela API oficial ao número WAHooks. Não inclui callback ou adaptador WAHooks dentro do SmartZap. | WAHooks → Meta oficial → webhook Meta → Inbox/IA → API Meta → WAHooks | aprovada |
| WHK-02 | Conversa multi-turno via WAHooks | Simular vários turnos no mesmo chat, mantendo contexto, confirmando e reformulando a resposta, acionando handoff seguro quando a pergunta fica sem fonte suficiente e retomando a IA sem criar loop. A rodada real de 03/08/2026 preservou o contexto de escola/Hotmart/planilhas/800 leads, transferiu para humano diante de preço e prazo não confirmados, suprimiu a resposta automática enquanto o modo humano estava ativo, retornou ao bot e respondeu uma FAQ fundamentada em um único outbound. | WAHooks → Meta oficial → webhook Meta → Inbox/IA ↔ WAHooks | aprovada |
| WHK-03 | Simulação comercial real via WAHooks | Prospecto começa sem histórico, pergunta sobre produto, API oficial, 2.000 contatos, opt-in, templates, audiência e entrega; a IA qualifica sem inventar preço e transfere intenção de contratação para humano | WAHooks → Meta oficial → Inbox/IA comercial | aprovada |
| MINI-01 | MiniApps | Listar, buscar, filtrar, atualizar e abrir | `/flows/builder` | aprovada |
| MINI-02 | MiniApps | Criar do zero, validar nome, editar, salvar e reabrir. Todo MiniApp recém-criado ou reaberto inicia no passo “Começar”, preservando as três escolhas do original: começar do zero, usar modelo pronto e criar com IA. | `/flows/builder` e `/flows/builder/:id` | aprovada |
| MINI-03 | MiniApps | Criar e validar cada template estático disponível. Como no legado, “Criar por template” abre um modal controlado por estado local dentro de `/flows/builder`, sem alterar a URL; “Criar MiniApp” na aba MiniApps de Templates cria um fluxo vazio e abre diretamente o editor. Dentro do passo “Começar” de `/flows/builder/:id`, “Usar modelo pronto” também deve permitir selecionar e aplicar um template ao fluxo aberto. | `/flows/builder`, `/flows/builder/:id` e `/templates` | aprovada |
| MINI-04 | MiniApps | Criar com IA pela lista e pelo passo “Começar” do editor; validar prompt, cancelamento, loading, erro, estrutura gerada e persistência | `/flows/builder` e `/flows/builder/:id` | aprovada |
| MINI-05 | MiniApps | Telas, blocos, regras, ramificações, ordem, preview e persistência. A matriz obrigatória inclui Título, Subtítulo, Texto, Legenda, texto curto (texto/e-mail/telefone/número), texto longo, data, dropdown, escolha única, múltipla escolha e opt-in; deve comprovar criação, edição, reordenação, exclusão, required/opções, múltiplas telas, destino padrão, regra condicional, tela final e prévia interativa. Em “Finalizar”, o nome continua editável, o mapeamento do contato é revisável e a publicação permanece explícita. Preservar o acesso a “Ajustes avançados” do editor original para manutenção estrutural de telas e rotas, incluindo atualização das referências ao renomear uma Screen ID. A matriz de 14 tipos, três telas, navegação, ramificação, preview, reordenação e exclusão de blocos, exclusão de tela, renomeação de Screen ID com atualização de `next`/regra e mapeamento final persistido passou em Chromium, Firefox e WebKit. | `/flows/builder/:id` | aprovada |
| MINI-06 | Meta Flows | Gerar e validar Flow JSON para cada componente e combinação suportada, criar draft, validar/publicar no provedor, enviar, receber callback e tratar rejeição. O contrato atual passou 71/71 testes focais; a Meta mantém um Flow `PUBLISHED` sem erro e duas submissões reais do destinatário autorizado chegaram a `completed`, com confirmação outbound registrada. | `/flows/builder/:id` | aprovada |
| MINI-07 | Meta Flows | Flow dinâmico com criptografia, `INIT`, `data_exchange` e persistência. O Worker de produção possui o par de chaves configurado; duas execuções reais registraram `INIT` e `data_exchange` concluídos, sem erro, e finalizaram a submissão. Google Calendar continua opcional e é rastreado separadamente em CAL-01. | `/flows/builder/:id` | aprovada |
| MINI-08 | MiniApps | Garantir um contrato único entre editor, persistência, gerador, validador e publicador. Todo bloco ou ação aceito deve chegar semanticamente íntegro ao Flow JSON. O contrato central recusa `DatePicker`, `PhotoPicker`, `DocumentPicker`, `open_url` e `update_data` antes de salvar/publicar e o renderer falha fechado, sem descarte silencioso. Ao excluir, drafts remotos são apagados e versões publicadas são depreciadas antes da remoção local; falha externa preserva o registro para reteste. | `/flows/builder/:id`, domínio e Meta Flows | aprovada |
| MINI-09 | MiniApps | Executar a matriz de fechamento funcional `qa/miniapps-functional-matrix.json`: três origens, oito modelos, 14 opções de bloco, limites e máximo + 1, telas, oito operadores, mapeamento, ciclo local/Meta, Flow dinâmico, combinações pairwise, três motores e cinco viewports. A execução de 04/08/2026 passou 147/147 casos, 27/27 E2E nos três motores sem retry, 3/3 smokes remotos e um canário Meta real publicado/enviado/limpo em staging. | `/flows/builder`, `/flows/builder/:id`, staging e Meta controlada | aprovada |
| MINI-10 | MiniApps | Estressar somente após MINI-08 e MINI-09: salvamento e publicação concorrentes, replay paralelo do endpoint, listagem volumosa, payload máximo e soak de criação → edição → publicação → submissão → cleanup. Em 04/08/2026, os seis grupos passaram no local isolado; cinco também foram repetidos no staging. O replay executou 20 chamadas paralelas com uma mutação e recuperação de claim abandonado no D1 isolado; o ciclo local completo publicou, enviou, submeteu e limpou oito vezes. No staging, conflito de revisão e trava de publicação foram explícitos, 220 itens foram paginados em três páginas, o payload de 24.443 bytes passou com máximo + 1 recusado e 20 ciclos CRUD não deixaram resíduo. O cleanup remoto removeu 243/243 artefatos; 642/642 testes de regressão e 6/6 E2E em Chromium/WebKit passaram. | local isolado e staging Cloudflare, versão `d3f30594-3c2c-42bb-a5c8-f2816a8ad981` | aprovada |
| MINI-11 | MiniApps | Homologação multidispositivo originalmente planejada para 24 combinações distintas nos aparelhos `…9966`, `…4524` e `…8242`. Em 04/08, os oito modelos obtiveram ao menos uma execução real verificada em `…9966`; `…4524` recebeu os oito sem abertura/submissão e `…8242` ficou em `sent` sem confirmação de entrega. Por decisão do operador, as 16 combinações restantes não serão repetidas e não fazem parte do gate atual. Essa descontinuação não altera MINI-07: o modelo suportado de agendamento dinâmico continua aprovado no escopo fechado de `INIT`, `data_exchange`, persistência, confirmação e integração opcional com Google Calendar; não representa um construtor de integrações arbitrárias. | staging Cloudflare ↔ Meta Cloud API ↔ WhatsApp dos destinatários autorizados | descontinuada |
| FORM-01 | Formulários | Criar, editar, campos, slug, publicar/inativar e excluir; menus de tag e tipo de campo usam componentes visuais consistentes, permitem criar tag no próprio editor e exibem a quantidade e o acesso direto às respostas. | `/forms` | aprovada |
| FORM-02 | Formulários | Validar e enviar formulário público, persistindo nome, telefone e e-mail; exigir consentimento explícito para WhatsApp, registrar a evidência e criar ou atualizar o contato com status opt-in. Cobrir erro, confirmação e indisponibilidade. | `/f/:slug` | aprovada |
| FORM-03 | Submissões | Listar, buscar, filtrar por origem, detalhar e exportar respostas de Forms públicos e MiniApps. Cada item deve identificar explicitamente a origem e o ativo que recebeu a resposta; no resumo, cada campo aparece em uma linha própria, sem concatenação por separadores; “Ver respostas” abre todos os campos em formato legível, sem JSON achatado ou truncado. O acesso pelo card ou cabeçalho de Forms abre as respostas já filtradas pelo formulário. | `/submissions` | aprovada |
| SET-01 | Configurações | Carregar IDs reais como valores editáveis (não como placeholders), mascarar segredos, validar, salvar, limpar/desconectar e recarregar configurações; ao salvar Phone ID/WABA, sincronizar templates automaticamente. Se a Meta falhar depois da persistência, manter os IDs salvos e retornar sucesso com aviso de sincronização, sem falso erro 502. | `/settings` | aprovada |
| SET-02 | Meta | Diagnóstico de credenciais, conexão, webhook, templates, conversões e erros. Fatos consultados no provedor são trivalentes: `true`/`false` somente após resposta conclusiva e `null` quando a verificação está indisponível. Limite temporário da Meta não pode ser exibido como token inválido, escopo ausente ou callback divergente; o piloto e a CAPI continuam bloqueados até nova confirmação. O bloco de Conversões verifica escopos, elegibilidade, Dataset associado à WABA e versão Graph sem interferir no envio normal de mensagens. | `/settings` e `/settings/meta-diagnostics` | aprovada |
| SET-03 | Performance | Métricas, filtros, períodos, throughput, vazio e erro | `/settings/performance` | aprovada |
| SET-04 | Configurações | Contato de teste, retenção da Inbox, timeout humano e persistência operacional | `/settings` | aprovada |
| SET-05 | Configurações | Uso da infraestrutura: backlog real das Queues, envios confirmados no mês, invocações mensais do Worker, storage e linhas lidas/escritas no D1 via token de Analytics com menor privilégio. Falha de acesso deve exibir indisponibilidade, nunca zero estimado. | `/settings` | aprovada |
| META-01 | Meta | Receber, persistir e responder por BSUID/username quando o telefone estiver presente ou omitido; status sem `recipient_id`. A prova oficial usa o cenário **User has adopted a username and phone number is unavailable** no App Dashboard da Meta e um envio real direcionado somente ao BSUID. O executor não adiciona parâmetro ou rota de QA ao Worker: exige staging canônico, runtime idêntico à release, destinatário autorizado com BSUID e conversa aberta, roda o contrato produtivo, envia uma única vez pela Graph API com `recipient` e sem `to`, registra a tentativa no ledger e restaura o callback global no `finally`. A homologação anterior `AUTOQA_META_BSUID_20260806114421_4fee8731` comprovou progressão `sent → delivered → read`. A revalidação exata `AUTOQA_META_BSUID_20260808_RELEASE_807`, ligada ao commit `8071c4499ab29f6f52065397ea50dc983de68117` e à versão Cloudflare `8c64633b-f6af-4b00-9b00-66e4cb3b2cb7`, comprovou evento oficial v26 sem telefone, username e BSUID persistidos, Inbox e evento idempotente únicos, resposta oficial aceita com uma chamada, `sent → delivered`, callback produtivo restaurado e cleanup sem resíduo. Como o primeiro executor encerrava ao receber `delivered`, a ferramenta de QA foi corrigida e coberta por teste para manter a rodada aberta até `read`. A rodada estrita `AUTOQA_META_BSUID_20260808_RELEASE_807_READ` repetiu toda a prova, registrou `sent → delivered` e encerrou sem `read`; restaurou o callback produtivo, removeu todos os artefatos temporários e foi corretamente marcada como reprovada. O reteste final `AUTOQA_META_BSUID_20260809_RELEASE_807_READ_2`, na mesma candidata imutável, observou novamente o evento oficial v26 sem telefone, persistiu username e BSUID, associou uma única conversa/mensagem, realizou exatamente uma chamada com `recipient` e sem `to`, recebeu `sent → delivered → read`, confirmou idempotência, restaurou o callback produtivo e terminou com zero contato, conversa, inbound, status ou rodada ativa residual; o ledger sanitizado único foi preservado. | webhooks, Inbox e envio | aprovada |
| CTWA-01 | Click-to-WhatsApp | Capturar `messages[].referral` antes do roteamento, persistir touchpoints mínimos por conversa e distinguir origem orgânica, CTWA atribuível, anúncio em Status sem `ctwa_clid`, múltiplos cliques e replay idempotente. Nunca tratar clique como opt-in de marketing, armazenar transcrição/mídia do anúncio por padrão ou expor o identificador integral em logs e interface. | `/webhook`, Queue e `/inbox/:id` | aprovada |
| CAPI-01 | Conversões Meta | Diagnosticar e ativar CAPI for Business Messaging sem afetar a Cloud API regular: verificar WABA/token, escopos `whatsapp_business_management` e `whatsapp_business_manage_events`, Marketing API Access Tier, Dataset associado e modelo de operação. No modo direto, exigir confirmação explícita de que somente a WABA própria é acessada; a documentação oficial dispensa Advanced Access e App Review nesse cenário. No modo parceiro, exigir acesso avançado a `whatsapp_business_manage_events` antes de operar WABAs de clientes. Consultar o Dataset antes de oferecer criação/associação com confirmação explícita e manter CAPI desligada quando qualquer fato obrigatório estiver falso ou desconhecido. | `/settings/meta-diagnostics` e Graph API | aprovada |
| CAPI-02 | Eventos comerciais | Registrar manualmente na Inbox `LeadSubmitted`, `QualifiedLead` e `Purchase` vinculados a um touchpoint atribuível e ao objeto comercial correto; exigir valor/moeda para compra, horário válido e chave idempotente, preservar trilha imutável e permitir correção por evento compensatório/cancelamento sem reescrever o fato original. A homologação preserva a separação entre o `LeadSubmitted` produtivo real e os três tipos processados no modo oficial Test Events; nenhum `QualifiedLead` ou `Purchase` comercial foi fabricado. | `/inbox/:id` e API de conversões | aprovada |
| CAPI-03 | Entrega CAPI | Persistir evento e outbox atomicamente, processar em Queue exclusiva, classificar aceite, falha temporária, falha permanente e resultado ambíguo, aplicar backoff/DLQ/recuperação por cron e deduplicar localmente antes da Meta. Enviar somente campos minimizados com `action_source=business_messaging`, `messaging_channel=whatsapp`, WABA e `ctwa_clid`, rejeitando evento com mais de sete dias e `test_event_code` fora de execução controlada. | D1, Queue, cron e `/{DATASET_ID}/events` | aprovada |
| CAPI-04 | Métricas de conversão | Exibir em `/analytics/conversions` visão geral, funil CTWA, eventos/falhas e diagnóstico, separando evento registrado, aceito pela API, matching individual e resultado atribuído agregado. O SmartZap não apresenta aceite HTTP como atribuição, não converte resultado agregado por anúncio/período em certeza sobre uma pessoa e não calcula CPA/ROAS sem gasto e receita reconciliados pela Marketing API. A tela deve identificar campanha, conjunto, anúncio, período, última sincronização e indisponibilidade do provedor, com alertas para atraso, falha, divergência ou configuração ausente. | `/analytics/conversions`, Inbox, Ads Insights e Events Manager | em teste |
| CAPI-05 | Homologação Meta | Homologar fixture assinada, concorrência, falhas e recuperação em local/staging; executar anúncio CTWA real autorizado, capturar `ctwa_clid`, enviar os três eventos, conferir o Dataset no Events Manager, validar canário Graph v26 e rollback para v25 antes da liberação gradual. No modo direto com WABA própria, não inventar bloqueio de App Review; no modo parceiro, só prosseguir após o Advanced Access aplicável. Test Events não é tratado como sandbox descartável. | staging Cloudflare, Graph API e Events Manager | em teste |
| CAPI-06 | Reconciliador Meta | Consultar periodicamente e sob demanda o Ads Insights da conta configurada usando autorização de leitura, persistir snapshots diários por anúncio e reconciliar somente em nível agregado conversa, lead, lead qualificado, compra, gasto e receita atribuída. O processo deve ser idempotente, paginado, sanitizado, tolerante a erro e limitado em frequência; deve manter o último snapshot válido quando a Meta estiver indisponível. Dataset Quality para eventos web não pode ser apresentado como qualidade do fluxo WhatsApp. | cron, D1, Marketing API e `/analytics/conversions` | corrigida — reteste pendente |
| CAL-01 | Integrações | Configurar OAuth Google com os escopos mínimos para consultar disponibilidade, listar agendas e criar eventos; conectar/desconectar, escolher calendário, agenda, slots e evento. Em staging, o consentimento real selecionou “EA - Trabalho”; uma submissão criptografada percorreu `INIT`, data, horário e confirmação, criou o evento real no Google Calendar e o artefato foi removido após a conferência. O endpoint autenticado `DELETE /api/google-calendar/qa-events/:eventId` existe somente em staging, aceita apenas IDs SHA-256 de 64 caracteres e torna o cleanup idempotente. | `/settings`, `/api/google-calendar/qa-events/:eventId` e MiniApp | aprovada |
| WEB-01 | Webhook | Verificação, assinatura, entrada, fila, idempotência, replay e erro; preservar e encaminhar o referral CTWA reconhecido sem incluir o payload publicitário bruto no conteúdo da mensagem | `/webhook` | aprovada |
| WEB-02 | Webhook | Status sent, delivered, read e failed sem regressão ou duplicidade | `/webhook` | aprovada |
| WEB-03 | Webhook | Payload até 3 MB, eventos de conta/qualidade/throughput e capacidade de entrega | `/webhook` e Queue | aprovada |
| SEC-01 | Segurança | Autorização, IDs, XSS, uploads, injeção, rate limit e proteção de segredos. A credencial remota de leitura aceita somente GET/HEAD em todos os ambientes; a credencial mutável de fixtures é aceita somente quando o runtime declara `staging` ou `test` e falha fechada em preview/produção, mesmo se um binding for configurado por engano. Tokens, `ctwa_clid`, texto/mídia e payloads pessoais não aparecem em logs de conversão; mutações de Dataset e Test Events exigem confirmação e ambiente elegível. | aplicação e APIs | aprovada |
| SEC-02 | Superfície descontinuada | Funcionalidades descontinuadas ou fora do escopo não podem manter UI morta nem APIs mutáveis montadas; Workflows responde `410 WORKFLOWS_RETIRED`, não consome Queue e preserva somente dados históricos | aplicação e APIs | aprovada |
| RSP-01 | Responsividade | Todas as rotas operacionais, inclusive Conversões e o diagnóstico CAPI, são auditadas em 360, 390, 620, 768, 1440 e 1920 px; telas críticas adotam mobile-first, toolbars quebram em largura reduzida, textos e controles não escapam dos próprios cards, Inbox alterna entre lista e conversa em tela cheia no mobile e tabelas permanecem em rolagem horizontal controlada quando necessário. A barra de filtros de Contatos mantém seus três controles dentro do viewport mínimo mesmo quando os filtros estão expandidos. | aplicação | aprovada |
| A11Y-01 | Acessibilidade | Teclado, foco, labels, contraste, zoom, modais e mensagens de erro; animações de status não podem reduzir a opacidade nem o contraste do texto. O gate integral percorre login, todas as rotas estáticas — incluindo Conversões e diagnóstico CAPI —, fixtures dinâmicas válidas, formulário público, portal do atendente, superfície aposentada e 404, além de exigir alerta semântico nos erros do portal. No menu móvel apresentado como diálogo modal, `Tab` e `Shift+Tab` permanecem contidos entre os controles do menu, `Escape` fecha a superfície e o foco retorna ao botão que a abriu. | aplicação | corrigida — reteste pendente |
| ERR-01 | Resiliência | Loading, vazio, timeout, 4xx/5xx, recuperação e Error Boundary nas rotas críticas; conversões distinguem falha local, rejeição Meta, indisponibilidade temporária e resultado ambíguo | aplicação | aprovada |
| OPS-01 | Operação | Health, filas, crons, reconciliação, retenção, limpeza e recuperação de falhas; incluir backlog, DLQ, sweep e retenção do pipeline CAPI sem misturá-lo à Queue de webhooks ou automação | Worker Cloudflare | aprovada |
| QA-01 | Controle autônomo de qualidade | Executar um controlador único que correlaciona versão, jornada, risco, testes, evidências e cleanup; oferecer gates `preflight`, unitário, contrato, E2E P0, matriz cross-browser, visual, IA, canário Meta e relatório consolidado; reprovar quando houver flake P0/P1, segredo, resíduo, erro interno do Worker ou evidência ausente. O controlador isola relatórios por navegador e reprova qualquer teste que só passe após retry; o eval de IA exige 100% em pass1, pass3, allAttempts e grounding factual; os grupos geral, Inbox, responsividade e WCAG usam processos independentes nos dois motores. No WebKit, o conjunto funcional geral e Projetos/Fábrica também usam processos independentes e finitos, preservando todas as asserções sem retry, para evitar que esgotamento acumulado de um único processo de navegador seja confundido com defeito do produto. O gate unitário executa os 58 arquivos do runtime Workers em dois shards determinísticos para não exceder a capacidade de um único processo `workerd`, e mantém a extração textual real de PDF em um projeto Node separado porque o fallback opcional nativo de canvas não é carregável pelo pool Workers; os dois projetos continuam obrigatórios no comando `npm test`. Monitores remotos preferem a credencial somente-leitura isolada e usam a chave administrativa apenas como fallback legado, evitando reprovação falsa após rotação da chave operacional. O parser do catálogo aceita letras e números no prefixo permanente, garantindo que `A11Y-01` e futuros IDs equivalentes não sejam omitidos da contagem nem da invalidação. O gate determinístico remoto passou sem flake; staging confirmou Worker, D1, health, autenticação, smoke, 84 sessões reais do Workers AI, cleanup e primeira prova do Cron; produção foi explicitamente pulada. | CLI/CI e ambientes local, preview e staging | aprovada |
| QA-02 | Isolamento de testes | Cada execução mutante recebe `run_id`, prefixo `AUTOQA`, fixtures, tag, journal e storage próprios, providers falsos por padrão e limpeza por IDs exatos; o gate local não chama Meta, Workers AI ou Google Calendar reais. App, inspetor, nome do Worker, Durable Objects e persistência são exclusivos por execução; concorrência entre o gate completo e a matriz cross-browser não pode gerar colisão nem erro interno. O cleanup também descobre órfãos e exige resíduo zero de agentes, documentos, campanhas, contatos e tags. | runtime de teste, D1/R2/Queues/DO locais e Playwright | aprovada |
| QA-03 | Canário Meta autônomo | Em staging, selecionar somente a quantidade solicitada entre os contatos autorizados, aplicar uma tag `AUTOQA` exclusiva por `run_id`, persistir o journal antes da mutação, respeitar allowlist, templates, janela e limites, correlacionar aceite, `wamid`, webhooks e timeline, validar inbound/IA/outbound e remover somente artefatos da própria execução. Contato real existente não é renomeado nem removido: com opt-in comprovado recebe apenas a tag temporária; quando o próprio titular autorizou explicitamente a homologação, uma flag separada permite registrar opt-in temporário somente no staging, preservando o status original no journal, recusando contatos suprimidos, revogando a evidência temporária e restaurando status/tag no cleanup. Qualquer violação aciona stop-the-line. O executor e os trocadores de callback preferem a credencial mutável isolada do staging e preservam a chave operacional somente como fallback legado. O teto diário é configurável entre 1 e 10 rodadas somente após autorização explícita do operador; fora de 09:00–20:00 BRT, uma execução exige autorização supervisionada separada e temporária no runner e no Worker. O CI troca os callbacks alternativos do número e da WABA apenas entre os dois Workers canônicos, exige três leituras consecutivas do número, WABA e callback efetivo antes de avançar, aguarda `delivered/read` mesmo depois de a campanha técnica ficar concluída, e restaura produção em `always` com a mesma prova de convergência, sem aceitar URL arbitrária; o callback global do app é preservado. A rodada `AUTOQA_META_20260805T205931Z_d097cf3` comprovou no número autorizado `…9966`: token e escopos válidos, qualidade `GREEN`, template `hello_world` aprovado, workflow aceito, `wamid`, progressão `pending → sent → delivered`, campanha removida, consentimento temporário revogado, status `unknown` e tag restaurados, e callback produtivo reconfirmado em três leituras. As jornadas separadas `WHK-01` e `WHK-03` comprovam o trecho inbound/IA/outbound no canal autorizado. | staging Cloudflare ↔ Meta Cloud API ↔ números autorizados | aprovada |
| QA-04 | Laboratório e promoção segura | Executar 28 cenários de IA em três tentativas, com graders determinísticos e juiz calibrado; pass1, pass3 e allAttempts exigem 100%, sem esconder flake por limiar. O grader também reprova resposta truncada, promessa de encaminhamento sem ação, função de Inbox não documentada, garantia de bloqueio e orientação de contato sem opt-in. A preparação do corpus aguarda o ciclo completo e finito da fila de indexação do AI Search antes de aprovar ou reprovar. A matriz responsiva divide as 20 rotas operacionais em grupos finitos de no máximo sete rotas, preserva todas as asserções e reprova qualquer retry. Promover preview → staging → canário → piloto somente com gates verdes, observabilidade e rollback ensaiado em até 10 minutos. Preview e staging usam credenciais próprias rotacionadas; preview mantém integrações externas inertes; toda mutação de staging é serializada. A versão corrigida `9a6e1282-fade-4924-aed5-d521c7db0542` repetiu no staging 28 cenários × 3 tentativas reais: **84/84**, com pass1, pass3, allAttempts, segurança, handoff e grounding em 100%, além de cleanup integral; a leitura semântica consultiva do Codex também aprovou **84/84** e permanece explicitamente separada da atestação humana. O mesmo runtime foi promovido à produção como `928fcd00-3bc3-4494-b4bd-f5cf5095c51f`, com health 4/4, filas e DLQ zeradas e canário WAHooks → Meta → SmartZap → Meta → WAHooks aprovado. A revisão humana possui artefato cego com **84 itens**, fingerprints da evidência, declaração nominal e verificador fail-closed. O soak de 14 dias foi encerrado em `AUTOQA_SOAK_FINAL_20260813_RETEST2`: **4.028/4.028** ciclos verdes, zero retry, backlog zero, nenhuma duplicidade e nenhuma lacuna inexplicada; esta jornada permanece aberta somente pelos gates humanos ainda não assinados. | CI, staging, Workers AI, Meta e piloto | em teste |
| QA-05 | Monitoramento autônomo do soak | A cada cinco minutos, um Worker observador isolado verifica pela entrada pública o health e o shell de staging e produção, aplica timeout e retry finitos, registra o resultado sanitizado com expiração, expõe somente o último estado não secreto e tolera a entrega pelo menos uma vez do Cron sem produzir efeitos no SmartZap. O monitor explicita a compatibilidade de fetch público necessária para consultar Workers da mesma zona; a política de CI impede sua remoção. O monitor externo do GitHub permanece como segunda perspectiva independente e somente leitura. Uma publicação manual normal exige os quatro alvos saudáveis; o modo de recuperação precisa ser escolhido explicitamente, ainda exige staging saudável e valida health e shell de produção depois da correção, evitando que uma falha preexistente impeça o próprio reparo. | Worker/Cron/KV/Analytics Engine isolados e GitHub Actions | aprovada |
| QA-06 | Certificação integral de produção | Homologar 100% das jornadas pertencentes ao escopo produtivo sobre uma versão imutável: suíte unitária, contratos, integrações, E2E, três motores, seis viewports, acessibilidade automatizada e manual, segurança e isolamento, carga/resiliência, cleanup, rotas públicas/autenticadas/dinâmicas, canários reais controlados, observabilidade, backup e rollback. O contrato de produção precisa apontar explicitamente para o diretório do build sanitizado, de modo que o comando de publicação usado pelo operador e pela automação falhe no gate, não durante a promoção. A certificação exige zero jornada ativa `não testada`, `em teste`, `bloqueada` ou `corrigida — reteste pendente`, zero P0/P1 conhecido e relatório final ligado ao commit, artefato e versão Cloudflare efetivamente publicados. O certificador fail-closed consulta ao vivo a versão e o tráfego Cloudflare, conta todo ID do catálogo, fixa hashes das evidências e exige 23 classes obrigatórias; arquivos ausentes, adulterados, de outro commit, com retry/flake/skip ou pertencentes a outra versão reprovam. Relatórios Playwright e health remoto carregam commit, versão e URL da release e são rejeitados se qualquer metadado divergir, impedindo reaproveitar uma rodada verde de outra publicação. A candidata imutável `8071c4499ab29f6f52065397ea50dc983de68117` está em produção como versão Cloudflare `8c64633b-f6af-4b00-9b00-66e4cb3b2cb7`; a prova exata aprovou **784/784** testes de regressão, **209/209** contratos, Chromium e Firefox com **133 aprovações e 2 skips intencionais** cada, todos os grupos WebKit sem falha ou retry, seis viewports, **6/6** visuais, **166/166** testes de segurança, **63** tabelas e **47** migrações restauradas com integridade `ok`, cleanup global com **82** ações e resíduo zero, 26 rotas remotas em três motores e três ciclos de health **4/4**. O gate manual de acessibilidade possui uma interface ligada à release com dez casos críticos, exige VoiceOver ou NVDA real, teclado, zoom de 200%, observação por caso, identificação e declaração pessoal; a interface apresenta um caso por vez, chama as ações de “Funcionou” e “Encontrei problema” e recolhe rotas técnicas em detalhes opcionais. O verificador reprova incompletude, adulteração, outra release ou qualquer falha. O aceite final de risco também possui interface e verificador fail-closed e só pode ser assinado depois das demais evidências. A reconciliação publicitária histórica foi fechada em `2` resultados de 07/08 fora da janela instrumentada e `3` resultados de 08/08 reconciliados com as três atribuições CTWA; a compatibilidade BSUID da release foi aprovada por evento oficial sem telefone, envio único e `sent → delivered → read`. O checkpoint atual registra **91/96 jornadas** e **19/23 evidências**; a certificação continua reprovada somente enquanto matching/atribuição CAPI, revisão humana das 84 respostas de IA, os dez casos manuais de acessibilidade, término temporal do soak e aceite final de risco permanecerem sem evidência conclusiva. | local isolado, staging e produção Cloudflare | em teste |
| WFL-01 | Workflows | Criar e editar workflow | `/workflows` | fora do escopo |
| WFL-02 | Workflows | Executar, reprocessar e auditar workflow | `/workflows/:id` | fora do escopo |
| COEX-01 | Coexistência | Onboarding e operação em coexistência Meta | integração Meta | fora do escopo |

## Lacunas abertas conhecidas

| Origem | Jornada afetada | Lacuna | Próxima evidência necessária |
| --- | --- | --- | --- |
| Calibração humana 09/08/2026 | AI-05 / QA-04 | As sete falhas foram corrigidas por limite fail-closed, retry finito, política de grounding e respostas determinísticas para Inbox, risco de bloqueio e opt-in de lista. A nova versão de staging passou 9/9 casos focais, 84/84 sessões reais e revisão semântica consultiva 84/84, com cleanup integral. O atestado nominal humano continua ausente e a revisão do Codex não se apresenta como substituto. | Revalidar a correção no canal real da versão candidata e, se o escopo formal continuar exigindo assinatura humana, concluir a revisão cega vinculada ao hash `fd36ef7d…` |
| Soak 13/08/2026 | QA-04 / QA-06 | O reteste final `AUTOQA_SOAK_FINAL_20260813_RETEST2`, ligado à release certificada, consolidou 4.028/4.028 ciclos verdes, p95 de 85 ms, zero retry, filas e DLQs sem backlog, nenhuma duplicidade, nenhuma lacuna inexplicada e zero P0/P1 conhecido | Evidência `soak-14-days` aprovada; não repetir salvo mudança de runtime, monitor ou release certificada |
| Acessibilidade manual 05/08/2026 | QA-06 | Scanner, contraste, alertas, viewports e casos de teclado automatizados passaram, mas a certificação integral ainda não possui atestado explícito de zoom 200%, leitor de tela e fluxos críticos manuais ligado à versão publicada | Executar o roteiro manual, registrar executor/data/rotas e obter todas as checagens obrigatórias sem bloqueio |
| CAPI/Events Manager 08/08/2026 | CAPI-04 / CAPI-05 | O Dataset registrou três eventos de servidor `LeadSubmitted`, um `QualifiedLead` e uma `Compra`; o diagnóstico está sem erro, mas a qualidade de correspondência ainda aguarda a janela de 24–48 horas e a Meta não exibiu conjunto de anúncios atribuído | Reconsultar qualidade, matching e atribuição no Events Manager e reconciliar com o evento produtivo sem converter HTTP 200 em atribuição |
| Reconciliação CTWA 08/08/2026 | CAPI-04 / CAPI-05 | No período **Hoje — 08/08**, Ads Manager e SmartZap reconciliam 3/3; no período máximo, a Meta exibe cinco resultados, deixando dois resultados históricos anteriores à janela CTWA observada sem vínculo materializado no SmartZap | Identificar os dois resultados históricos ou documentar formalmente a diferença de janela/critério da Meta sem misturá-los aos três cliques comprovados de 08/08 |
| Compatibilidade BSUID 08/08/2026 | QA-06 | A homologação oficial sem telefone está aprovada em versão anterior, mas a certificação exata da candidata não aceita reaproveitar o atestado como se pertencesse à release atual | Executar nova prova oficial na release atual ou produzir atestado de compatibilidade verificável, ligado ao diff dos caminhos BSUID e aos contratos exatos da candidata |
| Aceite final 05/08/2026 | QA-06 | O gate precisa de confirmação explícita do escopo, exclusões, ausência de P0/P1 e revisão das evidências após todas as demais provas | Produzir o atestado final somente depois que todas as jornadas e evidências anteriores estiverem aprovadas |
